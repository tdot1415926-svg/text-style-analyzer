# Tesseract is installed by the operating system (or included in the Docker
# image).  The Python package below is its small, CPU-compatible adapter.
pytesseract==0.3.13
opencv-python==4.6.0.66
numpy==1.26.4
Pillow==10.4.0
streamlit==1.39.0
pytest==8.3.3
